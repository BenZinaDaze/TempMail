# 后端环境变量配置

# Web 服务端口
PORT=3000

# SMTP 服务端口（开发环境用 2525，生产环境用 25 或 587）
SMTP_PORT=2525

# 邮箱域名（必需配置）
MAIL_DOMAIN=your-domain.com

# 前端跨域配置
CORS_ORIGIN=http://localhost:5173

# 邮箱有效期（分钟）
EMAIL_EXPIRY_MINUTES=60

# 邮箱前缀黑名单（逗号分隔，小写），不允许游客使用的前缀，如管理员标识
# 不配置则使用默认：admin,administrator,root,postmaster,webmaster,hostmaster,noreply,no-reply,support,info,abuse,security
# EMAIL_PREFIX_BLACKLIST=admin,administrator,root,postmaster

# 单个邮箱最大消息数（防止内存溢出）
MAX_MESSAGES_PER_EMAIL=50

# WebSocket 心跳间隔（毫秒），默认 30000（30秒）
# HEARTBEAT_INTERVAL=30000

# 过期邮箱清理间隔（毫秒），默认 60000（60秒）
# CLEANUP_INTERVAL=60000

# 日志配置
# LOG_LEVEL: 日志级别 (debug|info|warn|error)，默认 info
# LOG_LEVEL=info
