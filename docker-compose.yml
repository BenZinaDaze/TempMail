version: '3.8'

services:
  tempmail:
    image: benz1/tempmail:latest
    # build: .  <-- 如果需要本地构建，取消注释此行
    container_name: tempmail
    restart: always
    ports:
      # Web 访问端口 (外部:内部)
      - "3000:3000"
      # SMTP 邮件端口 (外部:内部)
      # 必须映射到外部的 25 端口才能接收来自其他邮件服务器的邮件
      - "25:2525"
    environment:
      # ==========================================
      # 👇 在这里修改你的域名配置
      # ==========================================
      - MAIL_DOMAIN=your-domain.com
      
      # 其他可选配置 (通常不需要修改)
      - PORT=3000
      - SMTP_PORT=2525
      - EMAIL_EXPIRY_MINUTES=60
      - MAX_MESSAGES_PER_EMAIL=50
      
      # 高级配置（可选，使用默认值即可）
      # CORS_ORIGIN: 跨域配置，默认 '*'（生产环境建议设置为具体域名）
      # - CORS_ORIGIN=*
      # HEARTBEAT_INTERVAL: WebSocket 心跳间隔（毫秒），默认 30000
      # - HEARTBEAT_INTERVAL=30000
      # CLEANUP_INTERVAL: 过期邮箱清理间隔（毫秒），默认 60000
      # - CLEANUP_INTERVAL=60000
      
      # 日志配置
      # NODE_ENV 已在 Dockerfile 中设置为 production（使用 JSON 格式日志）
      # LOG_LEVEL: 日志级别 (debug|info|warn|error)，默认 info
      # - LOG_LEVEL=info
