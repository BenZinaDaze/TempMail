# 📧 TempMail - 临时邮箱系统

一个功能完整的临时邮箱系统，支持实时接收邮件、WebSocket 推送和 60 分钟自动过期。

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Node](https://img.shields.io/badge/node-%3E%3D20-green.svg)
![Docker](https://img.shields.io/badge/docker-ready-blue.svg)

## ✨ 核心特性

- 📧 **临时邮箱生成** - 支持随机生成或自定义前缀
- 📎 **完整附件支持** - 支持接收和下载各种类型的附件
- 🎨 **现代化 UI** - 深色主题、优雅的动画交互
- 📱 **响应式设计** - 完美适配桌面和移动端

## 🏗️ 项目结构

```
Mail/
├── frontend/          # React + Vite 前端
│   ├── src/components/ # UI 组件 (邮箱列表、查看器、计时器)
│   ├── src/hooks/      # 自定义 Hooks (useWebSocket)
│   └── index.css       # 全局样式与变量
│
└── backend/           # Node.js 后端
    ├── src/server.js   # HTTP API 服务器
    ├── src/smtp.js     # SMTP 服务器 (基于 mailin)
    └── src/store.js    # 内存会话管理
```

## 🌐 域名与 DNS 设置

要让系统能接收邮件，需要配置 **A 记录**（服务器地址）和 **MX 记录**（邮件接收规则）。

| 类型   | 主机记录        | 记录值            | 说明                                 |
| :----- | :-------------- | :---------------- | :----------------------------------- |
| **A**  | `mail` (或 `@`) | `服务器IP`        | 指向服务器，确保只要能解析到 IP 即可 |
| **MX** | `@` 或者 `mail` | `上方A记录的域名` | 优先级 10，指向配置了 A 记录的域名   |

**配置说明：**

1.  **A 记录**: 必须确保 MX指向的域名（如 `mail.your-domain.com` 或 `your-domain.com`）能解析到服务器IP。
2.  **MX 记录**: 决定了邮箱后缀（`@your-domain.com`）的邮件由哪台服务器接收。

> **注意**: 必须开放服务器防火墙的 **25** (SMTP) 和 **3000** (Web) 端口。

## 🐳 Docker 部署 (推荐)

本项目提供了 `docker-compose.yml`，只需一步即可启动。

### 方式 1: 使用 Docker Compose (推荐)

1.  **修改配置**

    编辑 `docker-compose.yml`，修改 `MAIL_DOMAIN` 为您的域名：

    ```yaml
    environment:
      - MAIL_DOMAIN=your-domain.com # 👈 修改这里
    ```

2.  **启动服务**

    ```bash
    docker-compose up -d
    ```

### 方式 2: 使用 Docker Run

如果您不想使用 `docker-compose`，也可以直接运行：

```bash
docker run -d \
  --name tempmail \
  --restart always \
  -p 3000:3000 \
  -p 25:2525 \
  -e MAIL_DOMAIN=your-domain.com \
  benz1/tempmail:latest
```

服务启动后：

- **Web 访问**: `http://your-domain.com:3000`
- **SMTP 服务**: 监听 `25` 端口，准备接收邮件

## 📄 许可证

MIT License
